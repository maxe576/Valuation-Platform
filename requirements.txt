# --- Core app ---
streamlit>=1.36
pandas>=2.2
numpy>=1.26

# --- Data sources ---
requests>=2.32          # SEC EDGAR, FMP, FRED HTTP
# fredapi is optional; we use requests directly to stay dependency-light

# --- Modeling ---
scipy>=1.13             # reverse-DCF solver
# (justified-multiple regression uses numpy.linalg.lstsq — no statsmodels needed)

# --- Persistence ---
supabase>=2.4           # cloud persistence (Phase 8); local uses stdlib sqlite3

# --- Exports (Phase 9) ---
openpyxl>=3.1           # Excel export + reading legacy DCF workbook

# --- Config ---
python-dotenv>=1.0

# --- Testing ---
pytest>=8.0
